import dashboard from './dashboard';
import tool from './tools';
import widget from './widget';
// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, tool, widget]
};

export default menuItems;
